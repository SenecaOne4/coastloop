{
  "name": "coastloop-signage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "smoke:live": "node scripts/live-smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
